{
  "name": "vangmaya-wedding-invitation",
  "version": "1.0.0",
  "private": true,
  "description": "Pure Traditional Indian Digital Wedding Invitation Platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node server.js",
    "test:api": "cd backend && node test_api.js"
  }
}
